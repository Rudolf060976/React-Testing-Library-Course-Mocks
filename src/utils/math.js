export const add = (a, b) => a + b;

export const subtract = (a, b) => b - a;

export const multiply = (a, b) => a * b;

export const divide = (a, b) => b / a;

const doubleAndAdd = (a, b) => 2 * a + 2 * b;

export default doubleAndAdd;